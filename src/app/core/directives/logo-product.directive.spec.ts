import { LogoProductDirective } from './logo-product.directive';

describe('LogoProductDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoProductDirective();
    expect(directive).toBeTruthy();
  });
});
